b name / label / command (Dialog Dismiss) / params (title of dialog) / constraint / pos / depends
tf name / label / initVal / constraint / pos / depends
l name / label / initVal / constraint / pos / depends
s name / / value / pos / depends

check box
c name / label / initVal (t | f) / return type (i | v) / constraint / pos / depends
Choice (drop down menu) return type default is String selected 'i' returns the index
d name / label / comma delimeted list / return type / constraint / pos / depends

depends
false 
return of pos == String
return of pos >= int
return of pos <= int

Dialog title
end

constraint lxywh

Discuss the two different views of the data and form


Form Descriptor file: .npib file


